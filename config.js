module.exports = {
    development: {
        username: 'root', // Change this to your MySQL username
        password: 'Monivel2010', // Change this to your MySQL password
        database: 'Calendar-scheduler', // Change this to the name of your MySQL database
        host: 'localhost', // Change this if your MySQL server is running on a different host
        dialect: 'mysql' // Use 'mysql' as the dialect for MySQL
    },
    // You can add other environments like production, test, etc. if needed
};